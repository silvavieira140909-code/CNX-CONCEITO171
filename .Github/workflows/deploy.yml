Deploy site on: push branches traço main permissions contents dois pontos read pages write id-token write concurrency group pages cancel-in-progress true jobs deploy environment name github-pages url steps deployment underscore url runs-on ubuntu latest Steps name checkout uses actions/checkout arroba v4 Name setup node uses actions/setup-node arroba v4 with node-version 20 cache npm run npm ci run npm run build uses actions/configure-pages@v5
