import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {Glass} from '../components/Glass'; import {signIn,signUp} from '../lib/auth';
export function Auth(){const [register,setRegister]=useState(false); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [name,setName]=useState(''); const nav=useNavigate(); const submit=async(e:FormEvent)=>{e.preventDefault(); const r=register?await signUp(email,password,name):await signIn(email,password); if(r.error)alert(r.error.message); else nav('/')}; return <section className="section auth"><Glass><span className="eyebrow">VAULT PRIME</span><h1>{register?'Criar conta':'Entrar'}</h1><form onSubmit={submit}>{register&&<input placeholder="Nome" value={name} onChange={e=>setName(e.target.value)} required/>}<input type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="gold-btn">{register?'Criar conta':'Entrar'}</button></form><button className="link-btn" onClick={()=>setRegister(!register)}>{register?'Já tenho conta':'Criar uma conta'}</button></Glass></section>}
