import {useParams} from 'react-router-dom'; import {Glass} from './Glass'; import {startCheckout} from './payment';
export function Product(){const {id:routeId}=useParams(); const id=routeId??'download-demo'; const p={id,title:id==='video-demo'?'Mentoria Privada':id==='apk-demo'?'APK Premium':'Pack Digital',price:id==='video-demo'?79.9:id==='apk-demo'?29.9:39.9}; return <section className="section page"><Glass className="detail"><span className="eyebrow">PRODUTO</span><h1>{p.title}</h1><p>Área preparada para entrega de downloads, APKs, links externos e vídeos privados com URL assinada.</p><strong className="price">R$ {p.price.toFixed(2).replace('.',',')}</strong><button className="gold-btn" onClick={async()=>alert((await startCheckout([{id:p.id,title:p.title,price:p.price}])).message)}>Comprar</button></Glass></section>}
